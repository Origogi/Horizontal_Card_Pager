name: Flutter Test Web
on:
  push:
    branches:
      - master
jobs:
  build:
    name: Build Web
    env:
      my_secret: ${{secrets.commit_secret}}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: subosito/flutter-action@v1
        with:
          channel: 'dev'
      - run: flutter config --enable-web
      - run: flutter pub get
      - run: flutter build web --release
        working-directory: ./example_web
      - run: |
          cd example_web/build/web
          git init
          git config --global user.email rlawjdxo1212@naver.com
          git config --global user.name origogi
          git status
          git remote add origin https://${{secrets.commit_secret}}@github.com/Origogi/Horizontal_Card_Pager.git
          git checkout -b gh-pages
          git add --all
          git commit -m "update"
          git push origin gh-pages -f
